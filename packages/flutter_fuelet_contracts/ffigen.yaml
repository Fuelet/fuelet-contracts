# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: FlutterFueletContractsBindings
description: |
  Bindings for `src/flutter_fuelet_contracts.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/flutter_fuelet_contracts_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_fuelet_contracts.h'
  include-directives:
    - 'src/flutter_fuelet_contracts.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
